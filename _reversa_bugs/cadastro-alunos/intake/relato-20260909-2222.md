# Relato 20260909-2222

Usuário: adelino (chat/arte em pt-br)

## Relato (palavras do usuário)

> "Erro ao criar: new row violates row-level security policy for table 'alunos'"

## Observações do escrivão (investigação)

- Ponto de inserção: `src/app/alunos/novo/page.tsx:21` usa `supabase.from('alunos').insert({...})` com
  o client de navegador (sessão do usuário) e `criado_por: user.id`.
- Policy RLS que bloqueia: `supabase/migrations/001_schema.sql` linha 172
  `"Alunos insertable by VENDEDOR and SECRETARIA"` (FOR INSERT WITH CHECK), que só aceita
  `app_metadata.app_role IN ('VENDEDOR','SECRETARIA')`.
- GESTOR não está na policy de INSERT (está apenas na de SELECT). AUDITOR também não.
- Respostas do usuário:
  - A: usuário logado era **GESTOR** (`gestor@teste.local`).
  - B: login feito **depois** do seed/alteração de role (JWT fresco, claim `app_role` presente).
  - C: rota expressa (registro mínimo + correção na mesma passada).

## Situação

- A: gestor
- B: depois do seed (token com app_role é novo, não é token velho)
- C: expresso

## Esperado (desejo do usuário)

Criei o aluno, estava logado como GESTOR, e o cadastro foi concluído.

## Observado

Alert do frontend: "Erro ao criar: new row violates row-level security policy for table 'alunos'".

## Notas

- Spec `_reversa_sdd/sdd/cadastro-alunos-documentacao.md` seção 4 diz que GESTOR é somente leitura
  ("Edição direta bloqueada"), o que sugere que o bloqueio segue a spec do componente.
- Spec `_reversa_sdd/sdd/autenticacao-controle-acesso.md:107` permite GESTOR em `POST /api/v1/vendas`
  (apontamento). Não cobre criação de aluno diretamente.
- Veredito de spec é decisão humana no fix: permitir GESTOR (e AUDITOR?) inserir alunos
  (spec desatualizada) ou proibir o acesso do GESTOR à página de criação (spec correta).